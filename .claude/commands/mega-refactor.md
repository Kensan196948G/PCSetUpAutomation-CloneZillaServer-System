---
description: 全機能を使った大規模リファクタリング（安全・高速）
---

**メガリファクタリング**: 全機能を総動員して、プロジェクト全体を安全に刷新します。

## 🎯 目的

コードベース全体を**最新ベストプラクティス**に基づいて、安全かつ効率的にリファクタリングします。

---

## 📋 リファクタリング戦略

### フェーズ0: 現状分析（自動）- 10分

#### MCP: Serena → 完全スキャン
```
✓ 全ファイル解析（構造、依存関係）
✓ コード重複検出
✓ 複雑度測定
✓ デッドコード検出
✓ 命名規則チェック
```

#### MCP: Sequential Thinking → 問題分析
```
問題カテゴリ分類:
1. コード重複（DRY違反）
2. 長大な関数・メソッド
3. 複雑な条件分岐
4. 不明確な命名
5. アーキテクチャパターン違反
```

#### MCP: Context7 → 最新パターン確認
```
✓ Flask最新ベストプラクティス
✓ SQLAlchemy推奨パターン
✓ PowerShell最新規約
✓ REST API設計原則
```

**自動出力**:
- リファクタリング計画書
- 問題箇所リスト（優先度付き）
- 推定リスク評価

---

### フェーズ1: 安全性確保（自動）- 10分

#### SubAgent: test-engineer
**自動実行**:
```python
# リファクタリング前の全テスト実行
pytest tests/ --cov --cov-report=html

# 現状のベースライン確立
# - 全テスト結果
# - カバレッジ率
# - パフォーマンス指標
```

**自動出力**:
- `baseline-test-results.json`
- `baseline-coverage.html`
- `baseline-performance.json`

---

#### MCP: Serena → 参照関係マッピング
```
全シンボルの参照元を記録:
- 関数呼び出し関係
- クラス継承関係
- モジュールインポート関係

→ リファクタリング影響範囲の完全把握
```

**自動出力**: `reference-map.json`

---

#### Git: 安全ブランチ作成
```bash
# 自動実行
git checkout -b refactor/mega-refactor-$(date +%Y%m%d)
git commit -m "チェックポイント: リファクタリング開始前"
```

---

### フェーズ2: カテゴリ別リファクタリング（並列実行）- 60分

#### カテゴリ1: コード重複解消

**SubAgent: flask-backend-dev + api-developer**
**MCP: Serena**
```python
# 自動検出・解消

# 検出例:
"""
重複パターン1:
flask-app/views.py:45-60
flask-app/api.py:120-135
→ 共通関数化: utils/validators.py

重複パターン2:
（以下、全重複を自動検出）
"""

# 自動実行:
1. 重複コード検出
2. 共通関数抽出
3. 参照元を自動置換（Serena）
4. テスト実行で確認
```

**自動Git**:
```bash
git add .
git commit -m "refactor: コード重複解消 - XXX箇所を共通化"
```

---

#### カテゴリ2: 長大関数の分割（並列）

**SubAgent: powershell-scripter + flask-backend-dev**
**MCP: Serena + Context7**
```python
# 100行超の関数を自動検出・分割

# 検出例:
"""
長大関数1: setup_auto_kitting() - 250行
→ 以下に分割:
  - get_serial_number()
  - fetch_pc_info()
  - configure_computer_name()
  - apply_offline_domain_join()
  - run_post_setup()

長大関数2: （以下、全検出）
"""

# 自動実行:
1. 長大関数検出
2. 責務分析（Sequential Thinking）
3. サブ関数への分割
4. 元関数をサブ関数呼び出しに書き換え
5. テスト実行で確認
```

**自動Git**:
```bash
git add .
git commit -m "refactor: 長大関数分割 - XXX関数を分割"
```

---

#### カテゴリ3: 複雑な条件分岐の簡略化（並列）

**SubAgent: flask-backend-dev + powershell-scripter**
**MCP: Serena + Context7**
```python
# ネスト深い条件分岐を自動検出・簡略化

# パターン1: Early Return適用
# Before:
def process_pc(serial):
    if serial:
        if validate(serial):
            if exists(serial):
                # 処理
                return result
            else:
                return error
        else:
            return error
    else:
        return error

# After（自動変換）:
def process_pc(serial):
    if not serial:
        return error
    if not validate(serial):
        return error
    if not exists(serial):
        return error

    # 処理
    return result

# すべての複雑条件分岐を自動簡略化
```

**自動Git**:
```bash
git add .
git commit -m "refactor: 条件分岐簡略化 - Early Return適用"
```

---

#### カテゴリ4: 命名改善（並列）

**SubAgent: documentation-writer**
**MCP: Serena + Context7**
```python
# 不明確な命名を自動検出・改善

# 検出例:
"""
不明確な命名:
- a, b, x, tmp → 意味のある名前に
- getData() → getComputerInformation()
- process() → validateAndProcessPCSetup()

全XXX箇所を検出
"""

# 自動実行:
1. 不明確な命名検出
2. コンテキストから適切な名前を推論
3. 全参照元を一括置換（Serena: rename_symbol）
4. テスト実行で確認
```

**自動Git**:
```bash
git add .
git commit -m "refactor: 命名改善 - XXX箇所を明確化"
```

---

#### カテゴリ5: アーキテクチャパターン適用（並列）

**SubAgent: database-architect + flask-backend-dev**
**MCP: Serena + Context7 + Sequential Thinking**
```python
# アーキテクチャパターン適用

# パターン1: Repository パターン
# Before: views.py で直接 SQL
# After: repository層を導入

class PCRepository:
    def find_by_serial(self, serial):
        # DB操作

# パターン2: Service層の導入
# Before: views.py にビジネスロジック
# After: service層を導入

class PCService:
    def setup_computer(self, serial):
        # ビジネスロジック

# すべてのパターン適用を自動実行
```

**自動Git**:
```bash
git add .
git commit -m "refactor: アーキテクチャパターン適用 - Repository/Service層導入"
```

---

### フェーズ3: 継続的検証（各カテゴリ完了後）- 自動

#### 各リファクタリング後に自動実行

**SubAgent: test-engineer + integration-tester**
```python
# 1. 全テスト実行
pytest tests/ --cov

# 2. ベースラインと比較
assert test_results == baseline_test_results
assert coverage >= baseline_coverage

# 3. パフォーマンス確認
assert performance <= baseline_performance * 1.1  # 10%以内の劣化許容

# 失敗時は自動ロールバック
if any_test_failed:
    git reset --hard HEAD~1
    raise RefactoringError("テスト失敗：ロールバックしました")
```

**自動判定**:
- ✅ すべてOK → 次のカテゴリへ
- ❌ テスト失敗 → 自動ロールバック & 再試行

---

### フェーズ4: 最適化（自動）- 20分

#### SubAgent: devops-engineer + database-architect
**MCP: Serena + Sequential Thinking**

##### 1. データベース最適化
```python
# 自動実行:
1. N+1問題検出
2. 不要なクエリ削除
3. インデックス追加提案
4. クエリ最適化
```

##### 2. コード最適化
```python
# 自動実行:
1. 不要なループ検出
2. リスト内包表記適用
3. ジェネレータ活用
4. キャッシング導入
```

**自動Git**:
```bash
git add .
git commit -m "perf: パフォーマンス最適化 - XXX改善"
```

---

### フェーズ5: ドキュメント更新（自動）- 10分

#### SubAgent: documentation-writer
**MCP: Serena + Memory**
```markdown
# 自動生成:

## リファクタリングサマリ
- コード重複: 45箇所 → 0箇所
- 長大関数: 12個 → 0個
- 複雑条件分岐: 23箇所 → 簡略化完了
- 不明確命名: 67箇所 → 改善完了
- アーキテクチャパターン適用完了

## パフォーマンス改善
- API応答時間: 250ms → 150ms（40%改善）
- テストカバレッジ: 75% → 85%

## 主な変更点
（自動生成）
```

**自動出力**:
- `REFACTORING_REPORT.md`
- `ARCHITECTURE.md`（更新）
- コード内コメント自動更新

---

### フェーズ6: 最終検証（自動）- 10分

#### 全項目自動検証

**SubAgent: integration-tester**
**MCP: Puppeteer + Chrome DevTools**
```python
# 完全テストスイート実行

✅ ユニットテスト: 100% 成功
✅ 統合テスト: 100% 成功
✅ E2Eテスト: 全シナリオ成功
✅ パフォーマンステスト: 目標達成
✅ カバレッジ: 85% (ベースライン 75%)

# ベースラインとの比較
✅ すべてのテストが成功（改善前と同等以上）
✅ パフォーマンス: 40%改善
✅ コード品質: スコア 85 → 95
```

---

### フェーズ7: 統合・記録（自動）- 5分

#### Git: マージ準備
```bash
# 自動実行
git checkout main
git merge --no-ff refactor/mega-refactor-$(date +%Y%m%d) \
  -m "Mega Refactor: コードベース全面刷新

- コード重複解消
- 長大関数分割
- 条件分岐簡略化
- 命名改善
- アーキテクチャパターン適用
- パフォーマンス40%改善
- カバレッジ75%→85%

All tests passed ✅"
```

#### MCP: Memory → 永久記録
```
記録内容:
- リファクタリング日時
- 改善サマリ
- パフォーマンス指標
- テスト結果
- コード品質スコア
```

---

## 📊 期待される改善

### コード品質
```
Before → After
- コード重複: 45箇所 → 0箇所
- 長大関数: 12個 → 0個
- 複雑度: 平均25 → 平均8
- 命名問題: 67箇所 → 0箇所
```

### パフォーマンス
```
Before → After
- API応答時間: 250ms → 150ms（40%改善）
- ページロード: 1.5秒 → 0.8秒（47%改善）
- メモリ使用: 512MB → 320MB（38%削減）
```

### 保守性
```
Before → After
- テストカバレッジ: 75% → 85%
- コード品質スコア: 85点 → 95点
- ドキュメント整備率: 60% → 95%
```

---

## 🎯 使用例

### 全面リファクタリング
```
/mega-refactor
プロジェクト全体を最新ベストプラクティスに基づいて
完全リファクタリングしてください。
- コード品質向上
- パフォーマンス最適化
- 保守性改善
すべて自動で安全に実行してください。
```

### 特定モジュールのリファクタリング
```
/mega-refactor
flask-app/モジュールに絞ってリファクタリングしてください。
特にAPI実装部分を重点的に改善してください。
```

---

## ⏱ 所要時間

| フェーズ | 時間 |
|---------|------|
| 現状分析 | 10分 |
| 安全性確保 | 10分 |
| リファクタリング（並列） | 60分 |
| 最適化 | 20分 |
| ドキュメント | 10分 |
| 最終検証 | 10分 |
| 統合・記録 | 5分 |
| **合計** | **約125分** |

---

## 🔒 安全性保証

### 完全Git管理
- 各カテゴリ完了ごとにコミット
- テスト失敗時は自動ロールバック

### 継続的テスト
- 各リファクタリング後に全テスト実行
- ベースラインからの劣化を許さない

### 参照整合性保証
- Serenaで全参照元を追跡
- 一括置換で整合性確保

---

**全機能を総動員して、安全・高速・高品質なリファクタリングを実現します。**
