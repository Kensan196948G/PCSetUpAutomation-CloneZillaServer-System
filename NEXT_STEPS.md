# 📋 次のステップ（drblpush完了後）

**最終更新**: 2025年11月17日

---

## 🎯 現在の状況

drblpushの対話式設定を進めてください。すべての質問に **Enterキー**（デフォルト値）で回答してOKです。

### 推奨回答

| 質問 | 回答 |
|------|------|
| MACアドレス収集 | `N` |
| DRBLモード | Enter（デフォルト） |
| クライアント数 | `20` または Enter |
| その他すべて | **Enter**（デフォルト） |

---

## ✅ drblpush完了後に実行するコマンド

### ステップ1: DRBL設定自動修正（3分）

```bash
sudo ./FIX_DRBL_CONFIG.sh
```

**実行内容**:
- docker0 → enp2s0 に一括置換
- 関連設定ファイルすべてを修正
- DRBL設定再構成（drblpush -c）
- サービス再起動
- 動作確認

**所要時間**: 約3分

---

### ステップ2: 動作確認（1分）

```bash
# PXE環境診断
./CHECK_PXE_READINESS.sh

# 期待される結果:
# ✅ DHCP サーバ起動中
# ✅ TFTP サーバ起動中
# ✅ pxelinux.0 存在
# ✅ ファイアウォール設定OK
```

---

### ステップ3: PXEブート試行

#### 展開対象PC（192.168.3.139）で実施

1. **BIOS設定**
   - 電源ON → F2/F12/Del でBIOS画面
   - Boot順序: Network Boot → 最優先
   - Secure Boot: 無効化
   - F10（保存して終了）

2. **PXEブート試行**
   - PC再起動
   - 画面に「PXE」メッセージ表示を確認

3. **ログ監視**（別ターミナル）
   ```bash
   sudo tail -f /var/log/syslog | grep -E "dhcpd|tftpd"
   ```

**成功時の画面**:
```
>> Start PXE over IPv4.
  Station IP: 192.168.3.139
  Server IP: 192.168.3.135
  
PXELINUX 6.03 ...
Clonezilla Live Menu
```

---

## 🎊 完了

drblpushが完了したら、上記の手順を実行してください。

**最初に実行**: `sudo ./FIX_DRBL_CONFIG.sh`
